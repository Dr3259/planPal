{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Stores public information about a user.",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string"
        },
        "email": {
          "type": "string"
        },
        "photoURL": {
          "type": "string",
          "format": "uri"
        }
      }
    },
    "PlanData": {
      "title": "Plan Data",
      "description": "Generic container for all of a user's plan data.",
      "type": "object",
      "properties": {}
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/UserProfile"
      },
      "description": "User profile document."
    },
    "/plans/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/PlanData"
      },
      "description": "A single document holding all plan data for a user, keyed by fields like 'work_Daily_goals'."
    }
  }
}
